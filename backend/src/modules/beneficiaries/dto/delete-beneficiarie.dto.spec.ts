import { DeleteBeneficiarieDto } from './delete-beneficiarie.dto';

describe('DeleteBeneficiarieDto', () => {
  it('should be defined', () => {
    expect(new DeleteBeneficiarieDto()).toBeDefined();
  });
});
