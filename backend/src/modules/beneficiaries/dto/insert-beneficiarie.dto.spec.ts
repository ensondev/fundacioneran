import { InsertBeneficiarieDto } from './insert-beneficiarie.dto';

describe('InsertBeneficiarieDto', () => {
  it('should be defined', () => {
    expect(new InsertBeneficiarieDto()).toBeDefined();
  });
});
