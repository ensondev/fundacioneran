import { UpdateAvailabilityCoursesDto } from './update-availability-courses.dto';

describe('UpdateAvailabilityCoursesDto', () => {
  it('should be defined', () => {
    expect(new UpdateAvailabilityCoursesDto()).toBeDefined();
  });
});
