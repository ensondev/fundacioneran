import { InsertInstructorsDto } from './insert-instructors.dto';

describe('InsertInstructorsDto', () => {
  it('should be defined', () => {
    expect(new InsertInstructorsDto()).toBeDefined();
  });
});
