import { UpdateInstructorsDto } from './update-instructors.dto';

describe('UpdateInstructorsDto', () => {
  it('should be defined', () => {
    expect(new UpdateInstructorsDto()).toBeDefined();
  });
});
