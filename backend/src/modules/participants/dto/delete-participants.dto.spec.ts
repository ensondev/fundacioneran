import { DeleteParticipantsDto } from './delete-participants.dto';

describe('DeleteParticipantsDto', () => {
  it('should be defined', () => {
    expect(new DeleteParticipantsDto()).toBeDefined();
  });
});
