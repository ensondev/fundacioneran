import { InsertParticipantsDto } from './insert-participants.dto';

describe('InsertParticipantsDto', () => {
  it('should be defined', () => {
    expect(new InsertParticipantsDto()).toBeDefined();
  });
});
