import { DeleteRegistrationsDto } from './delete-registrations.dto';

describe('DeleteRegistrationsDto', () => {
  it('should be defined', () => {
    expect(new DeleteRegistrationsDto()).toBeDefined();
  });
});
