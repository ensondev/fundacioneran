import { InsertRegistrationsDto } from './insert-registrations.dto';

describe('InsertRegistrationsDto', () => {
  it('should be defined', () => {
    expect(new InsertRegistrationsDto()).toBeDefined();
  });
});
