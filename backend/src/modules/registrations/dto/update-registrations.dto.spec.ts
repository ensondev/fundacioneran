import { UpdateRegistrationsDto } from './update-registrations.dto';

describe('UpdateRegistrationsDto', () => {
  it('should be defined', () => {
    expect(new UpdateRegistrationsDto()).toBeDefined();
  });
});
