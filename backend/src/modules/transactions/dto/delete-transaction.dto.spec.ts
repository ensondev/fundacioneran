import { DeleteTransactionDto } from './delete-transaction.dto';

describe('DeleteTransactionDto', () => {
  it('should be defined', () => {
    expect(new DeleteTransactionDto()).toBeDefined();
  });
});
