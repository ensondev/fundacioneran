import { InsertTransactionDto } from './insert-transaction.dto';

describe('InsertTransactionDto', () => {
  it('should be defined', () => {
    expect(new InsertTransactionDto()).toBeDefined();
  });
});
